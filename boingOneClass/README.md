# Boing One Class

This a one class implementation of the boing game. It does not use inheritance, but rather relies on lambda functions to define the behavior of the game. The game is implemented in a single class, which makes it easy to understand and modify.
